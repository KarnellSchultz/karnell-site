import UseRef from 'content/posts/useRef.mdx';
import { Layout } from 'components/Layout';

export default function howTouseRef() {
  return (
    <Layout blog pageName={'How To useRef'}>
      <section>
        <UseRef />
      </section>
    </Layout>
  );
}
